<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.homescout.map.mapper.MapMapper">


    <select id="selectAllSafetyScore">
        select * from safety_score where sgg_cd like concat(#{sidoCd}, '%');
    </select>
</mapper>